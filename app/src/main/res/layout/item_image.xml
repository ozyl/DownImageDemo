<?xml version="1.0" encoding="utf-8"?>
<com.example.downimagedemo.SquareRelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/ivItem"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/tvItemSelected"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|top"
        android:layout_marginTop="10dp"
        android:layout_marginRight="10dp"
        android:height="30dp"
        android:background="@drawable/ic_selected_background"
        android:gravity="center"
        android:minWidth="30dp"
        android:paddingHorizontal="10dp"
        android:text="1"
        android:textColor="@color/white"
        android:visibility="invisible"
        android:textSize="20sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/ivItemSelect"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="end|top"
        android:layout_marginTop="10dp"
        android:layout_marginRight="10dp"
        android:src="@drawable/ic_select_background" />
</com.example.downimagedemo.SquareRelativeLayout>